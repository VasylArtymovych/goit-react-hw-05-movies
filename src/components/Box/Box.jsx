import styled from 'styled-components';
import { color, space, layout, flexbox, position } from 'styled-system';

const Box = styled('div')(color, space, layout, flexbox, position);

export default Box;
